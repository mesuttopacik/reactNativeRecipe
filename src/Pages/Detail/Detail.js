import { View, Text } from 'react-native'
import React from 'react'
import styles from './Detail.style'

const Detail = ({route}) => {
  return (
    <View style={styles.container}>
      <Text>detail</Text>
      <Text>Dont forget the yputube link</Text>
    </View>
  )
}

export default Detail